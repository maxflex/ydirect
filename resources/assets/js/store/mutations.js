export default {
    setUser(state, user) {
      state.user = user
    },
    setCampaign(state, campaign) {
      state.campaign = campaign
    },
    loading(state, value) {
      state.loading = value
    }
}
